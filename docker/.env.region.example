# =============================================================================
# GRT Intelligent System - Region-Aware Environment Configuration
# Version: 1.3.77
# =============================================================================

# =============================================================================
# 区域配置 (Region Configuration)
# =============================================================================
# 可选值: US (美国), DE (德国), CN (中国)
APP_REGION=US

# 节点标识 (用于跨节点同步)
SYNC_ORIGIN_NODE=US-HQ-01

# =============================================================================
# 服务端口配置
# =============================================================================
API_PORT=3000
DB_PORT=5432
REDIS_PORT=6379
OLLAMA_PORT=11434
HTTP_PORT=80
HTTPS_PORT=443

# =============================================================================
# 数据库配置
# =============================================================================
POSTGRES_USER=grt
POSTGRES_PASSWORD=your_secure_password_here
POSTGRES_DB=grt_system
DATABASE_URL=postgresql://grt:your_secure_password_here@grt-db:5432/grt_system

# =============================================================================
# 安全配置
# =============================================================================
JWT_SECRET=your_jwt_secret_here_min_32_chars
SYNC_HMAC_SECRET=your_sync_hmac_secret_here

# =============================================================================
# AI服务配置 (根据区域自动选择)
# =============================================================================
# 可选值: auto (自动根据区域选择), openai, deepseek, ollama, gemini
AI_PROVIDER=auto

# OpenAI (US区域默认)
OPENAI_API_KEY=sk-your-openai-api-key

# DeepSeek (CN区域默认)
DEEPSEEK_API_KEY=your-deepseek-api-key

# Gemini
GEMINI_API_KEY=your-gemini-api-key

# Ollama (离线环境)
OLLAMA_BASE_URL=http://host.docker.internal:11434

# =============================================================================
# S3存储配置
# =============================================================================
S3_ENDPOINT=https://s3.amazonaws.com
S3_BUCKET=grt-system-files
S3_ACCESS_KEY=your_s3_access_key
S3_SECRET_KEY=your_s3_secret_key

# =============================================================================
# 跨节点同步配置
# =============================================================================
SYNC_ENABLED=false
# 格式: node1_url,node2_url (逗号分隔)
SYNC_PEER_NODES=
# 同步间隔 (秒)
SYNC_INTERVAL=300

# =============================================================================
# 监控和告警配置
# =============================================================================
HEALTH_CHECK_INTERVAL=300
ALERT_WEBHOOK_URL=

# =============================================================================
# NPM镜像配置 (CN区域自动使用)
# =============================================================================
# 默认: https://registry.npmjs.org
# CN区域: https://registry.npmmirror.com
NPM_REGISTRY=https://registry.npmjs.org

# =============================================================================
# 其他配置
# =============================================================================
NODE_ENV=production
VERSION=1.3.77
API_REPLICAS=1
DEFAULT_ORGANIZATION_ID=grt-headquarters
GLOBAL_GATEWAY_URL=
