# ============================================================================
# GRT System Environment Variables - HMR Fix Configuration
# Windows 11 Local Development
# ============================================================================
# 
# 使用说明:
# 1. 将此文件内容复制到 .env.local
# 2. 或者运行: copy .env.local.hmr-fix .env.local
# 3. 重启开发服务器
#
# ============================================================================

# ============================================================================
# 数据库配置
# ============================================================================
DATABASE_URL=mysql://root:Gerry123456@localhost:3306/grt_dev

# ============================================================================
# Vite HMR 配置（Hot Module Replacement）
# 解决WebSocket连接失败导致的无限刷新问题
# ============================================================================

# HMR主机名 - 必须与浏览器访问的主机名一致
VITE_HMR_HOST=localhost

# HMR端口 - 必须与开发服务器端口一致
VITE_HMR_PORT=3000

# HMR协议 - 可选值: ws (WebSocket) 或 http (HTTP轮询)
# ws: 更快，但可能在某些网络环境下失败
# http: 更稳定，但性能略低
VITE_HMR_PROTOCOL=ws

# 禁用HMR（如果上述配置仍然不工作）
# 取消注释以下行来完全禁用HMR
# VITE_HMR_DISABLED=true

# ============================================================================
# OAuth 配置（演示模式）
# ============================================================================
VITE_APP_ID=demo-app-id
VITE_OAUTH_PORTAL_URL=http://localhost:3000/login
OAUTH_SERVER_URL=http://localhost:3000/api/oauth

# ============================================================================
# JWT 配置
# ============================================================================
JWT_SECRET=grt-local-dev-secret-key-change-in-production

# ============================================================================
# 服务器配置
# ============================================================================
NODE_ENV=development
PORT=3000

# ============================================================================
# 可选: Manus平台集成（如果需要）
# ============================================================================
# BUILT_IN_FORGE_API_URL=https://api.manus.im
# BUILT_IN_FORGE_API_KEY=your-api-key
# VITE_FRONTEND_FORGE_API_URL=https://api.manus.im
# VITE_FRONTEND_FORGE_API_KEY=your-frontend-api-key

# ============================================================================
# 故障排除配置
# ============================================================================

# 如果遇到 "Invalid frame header" 错误，尝试以下配置：

# 配置1: 使用HTTP轮询代替WebSocket
# VITE_HMR_PROTOCOL=http

# 配置2: 完全禁用HMR
# VITE_HMR_DISABLED=true

# 配置3: 增加HMR超时时间
# VITE_HMR_TIMEOUT=30000

# ============================================================================
# 调试配置
# ============================================================================
# 启用详细日志
# DEBUG=vite:*

# ============================================================================
# 浏览器配置
# ============================================================================
# 跳过浏览器自动打开
# VITE_SKIP_BROWSER=true
